# BookHub — قائمة المهام

## مكتمل سابقاً

- [x] إنشاء مشروع BookHub وربطه بالمصادقة وقاعدة البيانات.
- [x] بناء صفحات الكتب والإضافة والتفاصيل والتعديل والحذف.
- [x] إضافة البحث والتصفية والإحصائيات.
- [x] إضافة جدول `bookNotes` وتطبيق هجرته.
- [x] إضافة دوال قاعدة البيانات الأساسية للملاحظات.

## قيد التنفيذ

- [x] إضافة إجراءات tRPC للملاحظات والتقييمات الشخصية.
- [x] ربط الملاحظات والتقييمات بصفحة تفاصيل الكتاب.
- [x] اختبار عزل بيانات المستخدمين وصلاحيات القراءة والتعديل والحذف.
- [x] تشغيل `pnpm check` و`pnpm test` و`pnpm build`؛ نجحت الأوامر الثلاثة.
- [x] مراجعة الواجهة وتجربة الهاتف والسجلات؛ لا قص أو كسر أو تداخل في `/` و`/book/1` على سطح المكتب والهاتف، وعدد أخطاء query بعد `2026-08-17T10:09:00` هو 0. بقي تحذير chunk size وتحذير baseline-browser-mapping فقط.
- [x] حفظ checkpoint بعد اكتمال الاختبارات؛ الإصدار `1ff05d2a`.
- [x] رفع التغييرات المؤكدة إلى `gaidsalah311-alt/bookhub` على الفرع `manus/bookhub-notes-20260817`؛ تم تجنب الكتابة فوق `main` المتباين.
- [x] إرسال تقرير التسليم للمستخدم فعلياً؛ أُرسل رابط checkpoint وفرع GitHub في رسالة التسليم.

## معايير القبول

- كل عملية ملاحظة مرتبطة بالمستخدم الحالي وبكتاب يملكه.
- لا يستطيع مستخدم الوصول إلى ملاحظات مستخدم آخر.
- التقييم الشخصي اختياري ومحصور بين 1 و5.
- لا توجد مراجعات أو تقييمات وهمية أو بيانات مزروعة.
- حالات القراءة تبقى حرفياً: `مقروء`، `قيد القراءة`، `لم يُقرأ`.
- لا يتم النشر النهائي دون طلب صريح من المستخدم.

## قواعد العمل

- استخدام tRPC وDrizzle وفق بنية المشروع الحالية.
- استخدام `and()` و`eq()` لعزل البيانات في الاستعلامات.
- عدم تعديل ملفات البنية التحتية إلا عند الضرورة.
- تحديث هذه القائمة قبل checkpoint، وتعليم البنود المكتملة فقط بعد تحقق فعلي.

## سجل المتابعة

- [x] طلب المستخدم متابعة تطوير BookHub داخل GitHub.
- [x] استعادة المشروع من checkpoint `401ddae0`.
- [x] تنظيف ملف المهام بعد إنشاء نسخة مكررة بالخطأ.
- [x] إغلاق المهمة بعد تنفيذ الاختبارات وحفظ checkpoint ورفع التغييرات وإرسال رسالة التسليم للمستخدم.

## التحقق النهائي

- [x] TypeScript.
- [x] Vitest.
- [x] Build.
- [x] عزل البيانات.
- [x] مراجعة GitHub؛ فرع `main` الحالي مختلف عن نسخة BookHub المحلية، لذلك رُفعت التغييرات إلى فرع مستقل للمراجعة.

## ملاحظات التسليم

لا تُعتبر المهمة مكتملة إلا بعد توثيق التغييرات الفعلية ونتائج الفحوص وإرفاق checkpoint مناسب. لا تُحذف عناصر التاريخ من هذا الملف؛ تُحدّث حالتها أو تُضاف ملاحظات جديدة عند الحاجة.

## الحالة الحالية

تم التنفيذ: checkpoint محفوظ، والفرع المحدث مرفوع إلى GitHub، وتقرير التسليم أُرسل للمستخدم.

## فجوة تحقق مكتشفة قبل checkpoint

- [x] تشديد `bookNotes.upsert` لمنع payload يحتوي على `bookId` فقط أو ملاحظة فارغة بلا تقييم.
- [x] إضافة اختبارات Vitest لعزل المستخدمين، ورفض payload الفارغ، وحدود التقييم 1–5، ومنع الوصول إلى كتاب غير مملوك.

## خطأ سجلات مكتشف

- [x] جعل إجراءات `books.get` و`bookNotes.get` تعيد `null` بدلاً من `undefined` عند عدم وجود سجل، حتى لا تفشل TanStack Query.
- [x] إعادة تشغيل الفحوص ولقطات الواجهة بعد الإصلاح وتوثيق التحذيرات المتبقية؛ الفحوص نجحت واللقطات سليمة، والتحذيرات المتبقية غير حرجة.
